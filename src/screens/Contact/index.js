import './style.scss';
import Header from '../../components/Header';

const Contact = () => {
  return `
    ${Header()}
    <div class="hero contact">
      <h1>Contacts</h1>
    </div>
  `;
};

export default Contact;
